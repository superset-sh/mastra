---
'@mastra/playground-ui': patch
---

Fixed processor graph dialog scrolling — both the layer canvas and processor provider list are now independently scrollable when content exceeds the viewport height.
