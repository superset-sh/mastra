---
'@mastra/server': minor
---

Added HTTP API endpoints for harnesses. All registered harnesses are now accessible via REST routes including message streaming (SSE), thread management, mode/model switching, tool approval, state management, and permissions.
