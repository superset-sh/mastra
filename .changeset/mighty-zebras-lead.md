---
'@mastra/playground-ui': minor
---

Added dataset experiments comparison view with side-by-side experiment info, per-scorer summary table, per-item score deltas, and a detail panel for inspecting individual comparison items.

**Chip component enhancements**

Enhanced Chip component with new `intensity` prop for controlling brightness and additional color options.

