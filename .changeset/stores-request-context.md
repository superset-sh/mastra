---
'@mastra/server': minor
'@mastra/libsql': minor
'@mastra/pg': minor
---

Added `requestContext`, `defaultRequestContext`, and `requestContextSchema` fields to dataset server routes and storage adapters. New nullable columns are added via backward-compatible migrations.
